meta {
  name: Search Clients by Name
  type: http
  seq: 5
}

get {
  url: {{back}}/clients?search=Evelyn
  body: none
  auth: inherit
}

params:query {
  search: Evelyn
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("should return array of matching clients", function() {
    const data = res.body;
    expect(Array.isArray(data)).to.be.true;
  });
}
