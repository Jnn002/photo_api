meta {
  name: RemoveRole
  type: http
  seq: 16
}

delete {
  url: {{back}}/users/:user_id/roles/:role_id
  body: none
  auth: inherit
}

params:path {
  role_id: 2
  user_id: 10
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
