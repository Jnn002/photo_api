meta {
  name: AssignRole
  type: http
  seq: 12
}

post {
  url: {{back}}/users/:user_id/roles/:role_id
  body: none
  auth: inherit
}

params:path {
  role_id: 2
  user_id: 10
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
}
