meta {
  name: AssignRole
  type: http
  seq: 13
}

post {
  url: {{back}}/users/:user_id/roles/:role_id
  body: none
  auth: inherit
}

params:path {
  role_id: 4
  user_id: 11
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
}
