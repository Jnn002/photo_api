meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{back}}/auth/logout
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}

settings {
  encodeUrl: true
}
