meta {
  name: CreateRole
  type: http
  seq: 18
}

post {
  url: {{back}}/roles
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "name": "Test Role",
    "description": "Testing router"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
