meta {
  name: CreatePermission
  type: http
  seq: 20
}

post {
  url: {{back}}/permissions
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "code": "dashboard.view",
    "name": "Access Dashboard",
    "description": "Allow access to dashboard",
    "module": "Dashboard"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
