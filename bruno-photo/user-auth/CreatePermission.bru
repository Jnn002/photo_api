meta {
  name: CreatePermission
  type: http
  seq: 18
}

post {
  url: {{back}}/permissions
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "code": "session.payment",
    "name": "Registrar pagos de sesiones",
    "description": "Permite el registro de documentos de pago y su manajo",
    "module": "session"
  }
}

settings {
  encodeUrl: true
}
