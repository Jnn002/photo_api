meta {
  name: CreatePermission
  type: http
  seq: 20
}

post {
  url: {{back}}/permissions
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "code": "room.view",
    "name": "Ver rooms",
    "description": "Permite visualizar room",
    "module": "room"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
