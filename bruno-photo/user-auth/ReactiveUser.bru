meta {
  name: ReactiveUser
  type: http
  seq: 12
}

put {
  url: {{back}}/users/:reactive_user/reactivate
  body: none
  auth: inherit
}

params:path {
  reactive_user: 12
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
