meta {
  name: DeleteUser
  type: http
  seq: 11
}

delete {
  url: {{back}}/users/:delete_user
  body: none
  auth: inherit
}

params:path {
  delete_user: 12
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
