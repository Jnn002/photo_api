meta {
  name: UpdatePassword
  type: http
  seq: 10
}

patch {
  url: {{back}}/users/:user_id/password
  body: json
  auth: inherit
}

params:path {
  user_id: 9
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "current_password": "Admin1_admin",
    "new_password": "Admin1_admin"
  }
}

settings {
  encodeUrl: true
}
