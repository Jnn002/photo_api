meta {
  name: ListRoles
  type: http
  seq: 17
}

get {
  url: {{back}}/roles?active_only=true&limit=50&offset=0
  body: none
  auth: inherit
}

params:query {
  active_only: true
  limit: 50
  offset: 0
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
