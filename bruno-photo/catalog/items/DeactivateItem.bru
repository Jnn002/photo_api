meta {
  name: Deactivate Item
  type: http
  seq: 6
}

delete {
  url: {{back}}/items/:item_id
  body: none
  auth: inherit
}

params:path {
  item_id: 1
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("should return deactivated item", function() {
    const data = res.body;
    expect(data).to.have.property('status');
    expect(data.status).to.equal('Inactive');
  });
}
